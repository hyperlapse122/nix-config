.context/compound-engineering/
result
result-*
*.qcow2
*.img
.env
secrets/**/*.plain.*
secrets/**/*.key
__pycache__/
*.pyc

# update-dependencies.yml's Verify updates step writes these into the
# checkout root; the push step's catch-all `git add -A` would otherwise
# sweep them into main on every successful run.
/fmt.log
/check.log

# Auto-generated by dotagents — do not commit these files.
agents.lock
.agents/.gitignore

# dotagents
/.agents
/.claude
/.claude-plugin
/.opencode
